export type { ColorScheme, HljsColors, Mode, ThemePreferences } from './types';
export { DEFAULT_PREFERENCES } from './types';
export { ALL_SCHEMES, DARK_SCHEMES, LIGHT_SCHEMES, getScheme, getSchemesForMode } from './schemes';
export { applySchemeColors, applySchemeHljs, clearSchemeColors } from './applyScheme';
